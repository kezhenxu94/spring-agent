bootBuildImage {
  imageName = "ghcr.io/kezhenxu94/spring-agent"
  tags = ["latest", project.ext.gitSha].collect { "${imageName}:${it}" }

  // No --sun-misc-unsafe-memory-access here either: the option is JDK 24+, and this image runs on
  // the Java 21 pinned below, where it is a fatal "Unrecognized option".
  environment["BPE_APPEND_JAVA_TOOL_OPTIONS"] = "\
    --enable-native-access=ALL-UNNAMED \
    --add-opens=java.base/java.io=ALL-UNNAMED \
    --add-opens=java.base/java.lang=ALL-UNNAMED \
    --add-opens=java.base/java.time=ALL-UNNAMED \
    --add-opens=java.base/java.util=ALL-UNNAMED \
    --add-opens=java.base/sun.nio.ch=ALL-UNNAMED"

  // Pinned rather than left to the buildpack's inference: the build JDK is a GraalVM 25 so that
  // native-image is available, and this image must keep running on 21 regardless of what the build
  // ran on.
  environment["BP_JVM_VERSION"] = "21"

  builder = "paketobuildpacks/builder-jammy-full:latest"
  runImage = "paketobuildpacks/run-jammy-full:latest"
}
