新建一篇飞书云文档（docx），是空的，没有正文。它返回的 documentId 就是其他文档类工具要用的那个，
url 则是文档做好之后回复给用户的链接——如果还要写正文，就等正文写完再给。

**要写正文，把内容写成 Markdown 交给 FeishuWriteDocumentBody**，它一次调用就完成转换和插入，图片
也一并处理好。不要一块一块地拼。若要基于已有文档（比如一个模板）来创建，需要用云盘的复制接口，
这套工具目前还没有覆盖。
