把一个本地文件放进飞书云盘的文件夹，作为一个文件，并返回它的链接——把链接给提问的人即可。适用于
对方想把文件存到云盘、或者想以链接而不是聊天附件的形式分享；要发到会话里则用 FeishuSendFile。

文件原样存放，不做转换：这样上传的表格是一个可下载的 .xlsx，而不是谁都能打开编辑的飞书电子表格。
需要后者请用 FeishuImportFile。大文件会自动分片上传，无需额外指定什么；空文件会被拒绝。
