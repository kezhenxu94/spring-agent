执行一条 bash 命令，用于 npm、docker、make、mvn、python 之类的终端操作。

用法说明：
- command 参数是必填的。
- timeout 可选，单位毫秒（最大 600000 毫秒，即 10 分钟）。默认 120000 毫秒（2 分钟）。
- 长时间运行的命令请用 runInBackground。
- 含空格的文件路径要用双引号括起来。
- 有先后依赖的命令用 && 串起来；如果前面的失败可以接受，就用 ;。
- 优先使用绝对路径，而不是先 cd。
