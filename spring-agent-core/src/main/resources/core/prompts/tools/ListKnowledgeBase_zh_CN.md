列出知识库里已经存好的文档。

每个文档一行，不是每个分片一行；每行给出它的 id、标题、来源、被切成了多少个分片，以及什么时候
存进来的。每行还会说明它属于哪个知识库：当前用户自己的、当前群共享的，还是整个公司的。

用法：
- 取第一页时不带参数直接调用。
- 返回结果里会说明是否还有更多文档；如果有，把 offset 增加 limit 之后再调用一次。
- 返回的文档 id 可以交给 DeleteKnowledge 使用。
