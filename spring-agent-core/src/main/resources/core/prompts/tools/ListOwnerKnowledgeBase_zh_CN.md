列出另一个身份自己的知识库里存了哪些文档。仅管理员可用。

用来查看那些没有人会登录进去的身份名下都存了什么——比如某个事件源的 owner，它的知识库里放着
用来指导该源自动巡检的 playbook。这个 owner 是谁，可以用 ListPlaybooks 查看。

只列出该身份自己的知识库，也就是自动巡检真正读到的那个范围：群或公司共享给它的东西不会出现在
这里。要看你自己的，用 ListKnowledgeBase。

用法：
- 取第一页时只传 owner。
- 返回结果里会说明是否还有更多文档；如果有，把 offset 增加 limit 之后再调用一次。
