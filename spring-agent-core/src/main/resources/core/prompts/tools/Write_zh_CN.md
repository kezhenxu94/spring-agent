把一个文件写入本地文件系统。

用法：
- 如果给定路径上已经有文件，这个工具会覆盖它。
- 如果这是一个已存在的文件，你**必须**先用 Read 工具读过它的内容。没有先读就写，这个工具会失败。
- 永远优先修改代码库里已有的文件。除非确有必要，绝不新建文件。
- 绝不要主动创建文档文件（*.md）或 README。只有用户明确要求时才创建文档文件。
- 只有用户明确要求时才使用 emoji。除非被要求，不要往文件里写 emoji。
