替换某个已发布链接所提供的内容，而链接本身保持不变。

mode=update（默认）：把新内容叠加到已发布的内容之上，同名文件被覆盖，其余原样保留。只有当两边都是
目录时才可行，适用于新增或替换已发布内容中的一部分。
mode=replace：删掉此前发布的全部内容，把新内容放在它的位置上，相当于一次全新的发布，但沿用旧链接。

传入新的 ttl，有效期就从现在重新开始计算；不填则保持原有的到期时间。

新内容同样必须来自当前用户、当前群或公司的工作区之内。
