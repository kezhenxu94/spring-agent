从记忆库里删除一个文件或目录（及其全部内容）。

用法：
- 所有路径都相对于记忆库根目录。
- 删除目录会递归地连同其下所有文件和子目录一起删掉。
- 此操作不可撤销，请谨慎使用。
- 记忆库的根目录本身不能被删除。
- 删掉一个记忆文件之后，一定要用 MemoryStrReplace 把它在 MEMORY.md 里对应的条目也去掉，好让索引
  保持准确。
- 当一条记忆已被确认过时、错误或被取代时才用它 —— 不要就那样把过期条目留在那里。
