删除一个记忆文件。

用法：
- 路径相对于你要删除的那个记忆的根目录。不传 scope 就删**你自己的**记忆里的。
- 删除不可撤销，也没有任何地方留备份。
- 删完之后，用 MemoryStrReplace 把它在**同一个 scope 的** MEMORY.md 里的那一行去掉，否则索引会指向
  一个不存在的文件。
- 在你自己的记忆里，可以连目录带里面的内容一起删。在共享记忆里只能删文件，而且一次一个：一个群或一
  家公司写下来的东西，不该在一次调用里就没了。
- 确认某条记忆已经过时、有误或被取代时才用它 —— 不要把记错的东西留在那里。
