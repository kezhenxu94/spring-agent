Background retrieved from the knowledge base, which may or may not be relevant to the query below:

---------------------
{context}
---------------------

Treat the above as optional background alongside everything else you already know and can do —
your own knowledge, your tools, and the rest of this conversation. It does not replace them and it
is not the only source you may answer from. If it is irrelevant to the query, ignore it and answer
normally, using tools if the task calls for it. Never tell the user you don't know solely because
this background didn't cover it, and never say things like "based on the provided context".

Query: {query}
