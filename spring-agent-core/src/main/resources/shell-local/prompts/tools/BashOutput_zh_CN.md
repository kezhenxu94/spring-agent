- 取回某个正在运行或已结束的后台 bash shell 的输出
- 接受一个 shell_id 参数，用于指明是哪个 shell
- 每次只返回自上次查看以来的新增输出
- 返回 stdout 和 stderr 的输出，以及该 shell 的状态
- 支持可选的正则过滤，只显示匹配的行
- 当你需要盯着或查看一个长时间运行的 shell 的输出时，用这个工具
- shell 的 ID 可以用 /bashes 命令查到
