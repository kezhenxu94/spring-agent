# Corrections to the English these tools declare, which lives in spring-ai-agent-utils rather than
# here. Empty for now: nothing about the library's own wording is wrong, only untranslated, and the
# translations live in shell-local/prompts/tools/ beside this file.
#
# The key is <ToolName>.<parameterName>. See ModuleToolTexts for why the two kinds of text are split
# between a properties file and one markdown file per tool.
