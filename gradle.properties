# A release overrides this from the tag (`-Pversion=1.2.3`, see .github/workflows/maven.yaml). It
# must stay a -SNAPSHOT: GitHub Packages rejects a second upload of a release version, so a
# non-snapshot default would make the second push to main fail.
version=1.23.1-SNAPSHOT
