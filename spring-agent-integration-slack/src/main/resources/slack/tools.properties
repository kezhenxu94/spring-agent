# The English fallback. A tool's own description lives in its @Tool annotation; this file exists so
# that a parameter can be described in the deployment's language, and so a translation has a base to
# be a translation of. Keys are <ToolName>.<parameterName>.
