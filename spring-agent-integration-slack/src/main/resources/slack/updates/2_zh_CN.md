---
title: 你可以自己选谁来回答
---
输入 `/config` 会打开一个只有你看得见的弹窗：既可以在这个部署已经买单的模型里挑一个，也可以填入你自己的 OpenAI 兼容接口，让它来回答你的对话，别人仍然用默认的那个。这条路上不经过任何模型，所以哪怕你选的模型答不上话了，它照样能用；你的令牌会加密保存，之后不会再展示给任何人，包括你自己。

如果没有反应，说明这个命令还没在 Slack 应用里注册——发一条以空格开头的 ` /config`，同样的表单会直接在频道里打开。
