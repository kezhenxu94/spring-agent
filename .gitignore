.gradle/
build/
bin/
.classpath
.project
.factorypath
.settings/
*.DS_Store
.env
# Local chat memory and vector store files. Unanchored, so it covers both the module directory the
# tests run in and the repository root, which is where the native binary writes them.
data/
