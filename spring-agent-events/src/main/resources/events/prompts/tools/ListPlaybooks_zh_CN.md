列出本部署所监听的事件源，以及每个源的处理手册(playbook)是什么：它从哪个身份的知识库里读取、用哪个固定的问题检索、由哪条过滤条件决定哪些文档算数，以及那条过滤条件接受哪些文档 id。仅管理员可用。调用 WritePlaybook 之前先调用它，因为写在别的文档 id 下的 playbook 会保存成功，然后永远不会被读到。要读回某个源当前的 playbook 写了什么，把这里给出的 owner 传给 ListOwnerKnowledgeBase 或 SearchOwnerKnowledge——没有人会登录成那个身份，所以这是查看它的知识库的唯一办法。
