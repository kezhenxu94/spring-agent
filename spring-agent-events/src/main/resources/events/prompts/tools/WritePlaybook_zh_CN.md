写入或重写某个事件源的分诊运行所依据的手册(playbook)。仅管理员可用。

这段文字是无人值守的运行在做出任何判断之前，关于这个源所能看到的全部说明，所以要把它当作写给 agent 的指令来写：这类事件通常意味着什么、该看哪里、该做什么、该通知谁、以及哪些事没问过就绝对不能做。读它的那次运行身边没有人盯着，所以要写清楚哪些事它不能自己决定。

docId 必须是 ListPlaybooks 所说该源的过滤条件接受的 id 之一。用同一个 id 再写一次会替换那份 playbook；换一个 id 则会存下第二份文档，运行时会和第一份一起被读到。
